
After cloning this repository, do the following:

1) cd into the top level of the repo.
2) python3 -m venv venv  # this creates the virtual environment
3) . venv/bin/activate   # this activates the virtual environment
4) pip3 install -r requirements.txt # this intalls the dependencies into the virtual environment

5) build and run
